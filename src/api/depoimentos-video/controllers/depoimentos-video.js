'use strict';

/**
 *  depoimentos-video controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::depoimentos-video.depoimentos-video');
