'use strict';

/**
 * depoimentos-video service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::depoimentos-video.depoimentos-video');
